pre-commit:
  scripts:
    "pre-commit.sh":
      runner: bash
  commands:
    unit-tests:
      run: "stack test --ghc-options '-Werror'"
commit-msg:
  commands:
    commitlint:
      run: "commitlint --edit={1}"
